# Application Constants & Configuration Parameters

Centralized application configuration settings in client/src/config/.

export const CONFIG = {
  GAME_DURATION_SEC: 60,
  LOBBY_EXPIRY_MIN: 4,
  MIN_WORD_LENGTH: 3,
  DAILY_REWARD_CELO: "0.01",
  TREASURY_FEE_PERCENT: 10
};
